---
name: 1
description: "Sprint start (alias for /audit). Spec integrity, staleness scan, dependency verification, and initial CT setup. Phase /1."
---

### Frontmatter orientation

If the state file starts with a `---` YAML frontmatter block, read it first for session context from the previous session (`goal`, `now`, `done_this_session`, `decisions`, `next`). This provides fast orientation before reading the full state file.

Alias for `/audit`. Invoke the `audit` skill now using the Skill tool.
